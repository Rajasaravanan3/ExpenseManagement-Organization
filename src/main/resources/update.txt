remove all admin verifications

only admin can add budget, category

add admin can delete(or set isActive column) currency, paymentmethod, role and only admin can update and add currency, paymentmethod, role
add cascade to delete currency, paymentmethod, expense, role

verify all endpoints

enum type to string (comment it spring.jpa.hibernate.ddl-auto=validate at last and change enum type)


add spring security
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>